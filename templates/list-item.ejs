import * as React from 'react';
import { Text, View } from 'react-native';
import styles from './<%= props.name %>ItemStyle';

interface Props {
}

class <%= props.name %>Item extends React.PureComponent<Props> {
  public render() {
    return (
      <View style={styles.container}>
        <Text>Pure Component</Text>
      </View>
    );
  }
}
export default <%= props.name %>Item;
