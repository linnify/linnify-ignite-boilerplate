import * as React from 'react';
import { View, Text, FlatList } from 'react-native';
import <%= props.name %>Item from '../<%= props.name %>Item/<%= props.name %>Item';
import styles from './<%= props.name %>Style';


interface Props {
  data: <%= props.types %>[];
}

export default class <%= props.name %> extends React.PureComponent<Props> {

  renderItem = ({item}: {item: <%= props.types %> }) => {
    return <<%= props.name %>Item />
  };

  renderHeaderComponent = () => {
    return (
      <View>
        <Text> - Header - </Text>
      </View>
    )
  };

  renderFooterComponent = () => {
    return (
      <View>
       <Text> - Header - </Text>
      </View>
    );
  };

  renderEmpty = () => {
    return (
      <View>
        <Text> - Header - </Text>
      </View>
    );
  };

  renderItemSeparator = () => {
    return (
      <View>
        <Text> - Header - </Text>
      </View>
    );
  };

  keyExtractor = (item: <%= props.types %>, index: number) => {
    return index.toString();
  };

  render () {
    return (
      <View style={styles.container}>
        <FlatList
              contentContainerStyle={styles.listContent}
              data={this.props.data}
              renderItem={this.renderItem}
              keyExtractor={this.keyExtractor}
              ListHeaderComponent={this.renderHeaderComponent}
              ListFooterComponent={this.renderFooterComponent}
              ListEmptyComponent={this.renderEmpty}
              ItemSeparatorComponent={this.renderItemSeparator}
        />
      </View>
    )
  }
}