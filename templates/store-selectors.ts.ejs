import * as Types from '../state';
import {  <%= props.pascalName %>State } from '../reducers/<%= props.camelName %>.reducer';
import { createSelector } from 'reselect';

const get<%= props.pascalName %>State = (state: Types.RootState):  <%= props.pascalName %>State => state.<%= props.name %>;
